# mrp_consumption_warning

## Source system
This table originates from an Odoo ERP system, as evidenced by the naming convention of audit columns (`create_uid`, `write_uid`, `create_date`, `write_date`) and the use of Postgres sequence-based default values for the primary key, which are characteristic of Odoo's ORM layer.

## Functional process 
This table supports the Manufacturing Resource Planning (MRP) process, specifically tracking warnings or alerts generated during the consumption of raw materials for production orders. It likely logs instances where material usage deviates from the bill of materials (BOM) or inventory availability.

## Description
One row represents a single warning event triggered during the manufacturing consumption process. This is a raw landing table in the staging layer, containing audit metadata to track when and by whom each warning record was created or modified.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Unique identifier for the warning record | Primary key generated by sequence |
| create_uid | INTEGER | true | ID of the user who created the record | References the users table |
| write_uid | INTEGER | true | ID of the user who last modified the record | References the users table |
| create_date | TIMESTAMP | true | Timestamp of record creation | Assumed UTC |
| write_date | TIMESTAMP | true | Timestamp of last modification | Assumed UTC |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id` (Standard Odoo pattern for creator tracking)
    - `write_uid` → `res_users.id` (Standard Odoo pattern for modifier tracking)
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- Timestamps (`create_date`, `write_date`) are assumed to be in UTC, consistent with standard Odoo database configurations.
- This table contains audit metadata but lacks the business-specific columns (e.g., `mrp_production_id`, `warning_message`) typically expected in a functional table; ensure the source extraction process is capturing the full schema.
- No soft-delete flag is present; assume records are either hard-deleted or maintained indefinitely.