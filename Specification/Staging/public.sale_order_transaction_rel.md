# sale_order_transaction_rel

## Source system
The table likely originates from a relational ERP or e-commerce backend system, such as Odoo or a custom-built PostgreSQL-based order management system. The naming convention `_rel` is highly characteristic of join tables generated by ORMs (Object-Relational Mappers) to manage many-to-many relationships between sales orders and financial transactions.

## Functional process 
This table supports the order-to-payment reconciliation process. It maps individual sales orders to their corresponding financial transactions, allowing the system to track which payments or credit memos have been applied to specific customer orders.

## Description
One row in this table represents a single association between a sales order and a financial transaction. It serves as a raw landing join table in the staging layer, facilitating the resolution of many-to-many relationships between the `sale_order` and `transaction` entities.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| transaction_id | INTEGER | false | Foreign key to the transaction entity | Represents the unique identifier of the financial transaction. |
| sale_order_id | INTEGER | false | Foreign key to the sales order entity | Represents the unique identifier of the sales order. |

## Keys

- **Primary key (inferred):** Not confidently inferable; likely a composite primary key on `(transaction_id, sale_order_id)`.
- **Foreign keys (inferred):** 
    - `transaction_id` → `transaction.id`: Links to the financial transaction record.
    - `sale_order_id` → `sale_order.id`: Links to the sales order record.
- **Natural keys (inferred):** The combination of `(transaction_id, sale_order_id)` acts as the business key for this relationship.

## Caveats for downstream consumers

- This is a bridge table; queries should expect to join this with both the `sale_order` and `transaction` tables to retrieve meaningful business attributes.
- There are no timestamps or audit columns present; it is impossible to determine the sequence of events or the ingestion time from this table alone.
- The table does not contain soft-delete flags; assume that the presence of a row indicates an active relationship in the source system.