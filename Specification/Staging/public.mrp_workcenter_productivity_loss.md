# mrp_workcenter_productivity_loss

## Source system
This table originates from an Odoo ERP system, as evidenced by the naming convention (`mrp_workcenter_productivity_loss`), the use of `create_uid`/`write_uid` audit columns, and the `JSONB` type for the `name` field, which is characteristic of Odoo's multi-language field storage.

## Functional process 
This table supports the Manufacturing Resource Planning (MRP) module, specifically tracking productivity losses at work centers. It defines the categories or reasons for downtime or efficiency loss, allowing production managers to categorize why a work center is not operating at peak capacity.

## Description
One row in this table represents a specific definition or category of productivity loss that can be assigned to a work center. It serves as a raw landed copy of the Odoo configuration table, capturing the metadata and classification of loss events used in manufacturing performance reporting.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by the source system. |
| sequence | INTEGER | true | Display order index | Used to sort loss types in the UI. |
| loss_id | INTEGER | true | Reference to loss category | Likely a foreign key to a parent loss category table. |
| create_uid | INTEGER | true | Creator user ID | References the user who created this record. |
| write_uid | INTEGER | true | Last modifier user ID | References the user who last updated this record. |
| loss_type | VARCHAR | true | Classification of loss | e.g., 'availability', 'performance', or 'quality'. |
| name | JSONB | false | Display name | Multilingual label stored as a JSON object. |
| manual | BOOLEAN | true | Manual entry flag | Indicates if this loss type is manually triggered. |
| create_date | TIMESTAMP | true | Creation timestamp | Recorded in UTC by the source system. |
| write_date | TIMESTAMP | true | Last update timestamp | Recorded in UTC by the source system. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id` (guess: standard Odoo audit pattern).
    - `write_uid` → `res_users.id` (guess: standard Odoo audit pattern).
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- The `name` column is `JSONB`; ensure your query logic handles extraction (e.g., `name->>'en_US'`) to retrieve human-readable text.
- Timestamps (`create_date`, `write_date`) are assumed to be in UTC, consistent with standard Odoo database configurations.
- This table is a configuration/lookup table; it does not contain transactional event logs, but rather the definitions used to categorize those logs.
- No explicit soft-delete flag is present; assume standard Odoo behavior where records are either active or removed.