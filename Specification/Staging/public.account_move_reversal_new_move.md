# account_move_reversal_new_move

## Source system
The table likely originates from an Odoo ERP system. The naming convention `account_move_reversal_new_move` is characteristic of Odoo's accounting module, which uses "account moves" to track ledger entries and maintains specific link tables to manage the relationship between a reversed journal entry and its corresponding reversal entry.

## Functional process 
This table supports the accounting audit trail and ledger correction process. It maintains the relational mapping between an original accounting move that has been reversed and the new "reversal" move created to offset it, ensuring that financial reporting can accurately trace the lifecycle of corrected transactions.

## Description
One row in this table represents a single link between an original accounting move and its associated reversal move. As a staging table, it serves as a raw, landed copy of the relationship mapping extracted directly from the source ERP database to facilitate downstream reconciliation and ledger analysis.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| reversal_id | INTEGER | false | The unique identifier of the reversal move. | References the primary key of the account move that performs the reversal. |
| new_move_id | INTEGER | false | The unique identifier of the new move. | References the primary key of the accounting entry being created or linked. |

## Keys

- **Primary key (inferred):** Not confidently inferable. While `reversal_id` and `new_move_id` likely form a composite primary key, the metadata does not explicitly define one.
- **Foreign keys (inferred):** 
    - `reversal_id` → `account_move.id`: Guessed based on Odoo standard schema naming conventions for accounting moves.
    - `new_move_id` → `account_move.id`: Guessed based on Odoo standard schema naming conventions for accounting moves.
- **Natural keys (inferred):** The combination of `(reversal_id, new_move_id)` is the business key identifying the relationship between these two specific ledger entries.

## Caveats for downstream consumers

- This table contains no timestamps or audit metadata; it is a pure relational mapping.
- The table does not contain financial values or account codes; it must be joined with the `account_move` table to retrieve transaction details.
- Assumes that `reversal_id` and `new_move_id` are surrogate keys generated by the source system's database sequence.