# res_partner_category

## Source system
This table originates from Odoo (formerly OpenERP), an open-source ERP system. The naming convention `res_partner_category` and the presence of specific columns like `parent_path`, `create_uid`, and `write_uid` are characteristic of Odoo's internal ORM structure for managing hierarchical partner tags or categories.

## Functional process 
This table supports the Customer Relationship Management (CRM) and Partner management processes. It defines the taxonomy used to categorize partners (customers, vendors, or leads), allowing for hierarchical grouping via the `parent_id` and `parent_path` fields to facilitate complex reporting and segmentation.

## Description
One row in this table represents a single category or tag used to classify business partners. It is a raw landed copy of the Odoo `res.partner.category` model, capturing the category name, its hierarchical position in the tree, and audit metadata.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| color | INTEGER | true | UI color index | Used for visual grouping in the Odoo frontend. |
| parent_id | INTEGER | true | Self-referencing foreign key | Points to the parent category for hierarchy. |
| create_uid | INTEGER | true | Creator user ID | References the user who created the record. |
| write_uid | INTEGER | true | Last modifier user ID | References the user who last updated the record. |
| parent_path | VARCHAR | true | Materialized path | String representation of the hierarchy tree. |
| name | JSONB | false | Category name | Multilingual support via JSONB structure. |
| active | BOOLEAN | true | Soft-delete flag | If false, the category is hidden from the UI. |
| create_date | TIMESTAMP | true | Creation timestamp | Recorded in UTC by the Odoo application. |
| write_date | TIMESTAMP | true | Last update timestamp | Recorded in UTC by the Odoo application. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `parent_id` → `public.res_partner_category.id`: Defines the parent-child relationship within the category tree.
    - `create_uid` → `public.res_users.id` (guess): Likely references the system user who created the record.
    - `write_uid` → `public.res_users.id` (guess): Likely references the system user who last modified the record.
- **Natural keys (inferred):** Not confidently inferable. While `name` is descriptive, Odoo typically relies on the surrogate `id` for internal linking.

## Caveats for downstream consumers

- **PII/Sensitivity:** This table contains no direct PII, but `create_uid` and `write_uid` link to internal user identities.
- **Timezones:** Timestamps (`create_date`, `write_date`) are typically stored in UTC by Odoo.
- **Soft Deletes:** The `active` column acts as a soft-delete flag; ensure queries filter by `active = true` unless historical/archived data is required.
- **JSONB:** The `name` column is a `JSONB` object; use PostgreSQL `->>` operator to extract the string value (e.g., `name->>'en_US'`).