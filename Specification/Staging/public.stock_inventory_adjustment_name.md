# stock_inventory_adjustment_name

## Source system
This table originates from an Odoo ERP system. The naming convention of columns such as `create_uid`, `write_uid`, `create_date`, and `write_date`, combined with the use of PostgreSQL sequence-based defaults (`nextval`), is characteristic of Odoo's ORM-generated audit and tracking fields.

## Functional process 
This table supports the inventory management module, specifically tracking the naming or labeling conventions for inventory adjustments. It acts as a lookup or configuration table that allows users to categorize or name specific stock adjustment events, likely used to provide context for stock reconciliation processes.

## Description
Each row represents a unique identifier and descriptive label for an inventory adjustment category or name. As a staging table, it serves as a raw, direct copy of the source system's configuration entity, intended to provide downstream models with the mapping between adjustment IDs and their human-readable names.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by PostgreSQL. |
| create_uid | INTEGER | true | User ID who created the record | References the system's user table. |
| write_uid | INTEGER | true | User ID who last modified the record | References the system's user table. |
| inventory_adjustment_name | VARCHAR | true | Descriptive name of the adjustment | The business-facing label for the adjustment. |
| create_date | TIMESTAMP | true | Record creation timestamp | Likely in UTC. |
| write_date | TIMESTAMP | true | Last modification timestamp | Likely in UTC. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id` (Guess: standard Odoo pattern for tracking record creators).
    - `write_uid` → `res_users.id` (Guess: standard Odoo pattern for tracking record modifiers).
- **Natural keys (inferred):** Not confidently inferable.

## Caveats for downstream consumers

- Timestamps (`create_date`, `write_date`) are assumed to be in UTC, consistent with standard Odoo database configurations.
- This table does not appear to implement soft deletes; standard Odoo tables typically use `active` boolean flags for that purpose, which are absent here.
- The `inventory_adjustment_name` column may contain duplicates if the source system allows non-unique naming conventions.
- `create_uid` and `write_uid` should be treated as nullable, as system-level migrations or automated scripts may not always be linked to a specific user ID.