# picking_label_type_stock_picking_rel

## Source system
This table likely originates from an Odoo ERP system. The naming convention `_rel` combined with the specific association of `picking_label_type` and `stock_picking` is characteristic of Odoo's many-to-many relationship tables generated by the ORM to link inventory picking operations with specific label configuration types.

## Functional process 
This table supports the warehouse management and logistics process, specifically the generation of shipping or inventory labels. It maps individual stock picking operations to the specific label formats or types required for those shipments, ensuring that the correct label templates are applied during the fulfillment workflow.

## Description
One row in this table represents a single association between a stock picking operation and a label type. It acts as a join table in a many-to-many relationship, allowing a single picking to be associated with multiple label types or vice versa. As a staging table, it provides a raw, normalized link between these two entities for downstream transformation.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| picking_label_type_id | INTEGER | false | Foreign key to the label type definition | References the configuration of the label. |
| stock_picking_id | INTEGER | false | Foreign key to the stock picking record | References the specific warehouse movement. |

## Keys

- **Primary key (inferred):** Not confidently inferable; likely a composite primary key consisting of `(picking_label_type_id, stock_picking_id)`.
- **Foreign keys (inferred):** 
    - `picking_label_type_id` → `picking_label_type.id` (Inferred from naming convention).
    - `stock_picking_id` → `stock_picking.id` (Inferred from naming convention).
- **Natural keys (inferred):** The combination of `picking_label_type_id` and `stock_picking_id` acts as the business key for this relationship.

## Caveats for downstream consumers

- This is a junction table; expect no descriptive attributes other than the two foreign keys.
- There are no timestamps or audit columns present; incremental loading logic should rely on the source system's transaction logs or full-table snapshots.
- Ensure referential integrity checks are performed against the parent tables (`stock_picking` and `picking_label_type`) as this table may contain orphaned records if the source system does not enforce strict cascading deletes.