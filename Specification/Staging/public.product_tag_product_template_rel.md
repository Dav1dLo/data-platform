# product_tag_product_template_rel

## Source system
This table likely originates from an Odoo ERP or a similar modular Python-based framework, as indicated by the `_rel` suffix and the naming convention `product_tag_product_template_rel`, which is characteristic of many-to-many join tables generated by Object-Relational Mappers (ORMs) in these systems.

## Functional process 
This table supports the product categorization and tagging process. It facilitates a many-to-many relationship between product templates (the base definition of a product) and product tags, allowing a single product to be associated with multiple descriptive tags for filtering, reporting, or e-commerce storefront organization.

## Description
One row in this table represents a single association between a specific product template and a specific product tag. This is a raw landing of a join table, serving as the bridge to resolve the many-to-many relationship between products and their metadata tags.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| product_template_id | INTEGER | false | Foreign key to the product template | Represents the unique identifier of the product. |
| product_tag_id | INTEGER | false | Foreign key to the product tag | Represents the unique identifier of the tag. |

## Keys

- **Primary key (inferred):** The composite key of `(product_template_id, product_tag_id)`.
- **Foreign keys (inferred):** 
    - `product_template_id` → `product_template.id`: This column links to the master product definition table.
    - `product_tag_id` → `product_tag.id`: This column links to the tag definition table.
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- This table is a pure join table; it contains no descriptive attributes other than the two foreign keys.
- Queries should expect a composite primary key; ensure joins are performed on both columns to avoid Cartesian products.
- There is no audit or timestamp information available in this table to determine when the association was created or modified.