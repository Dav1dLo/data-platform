# sale_order_template_option

## Source system
This table originates from an Odoo ERP system. The naming convention (`sale_order_template_option`), the use of `create_uid`/`write_uid` for audit tracking, and the `JSONB` type for localized fields are characteristic of Odoo's PostgreSQL-based backend architecture.

## Functional process 
This table supports the "Quote Template" or "Sales Quotation Template" business process. It stores optional products or services that can be added to a sales quotation template, allowing users to pre-configure bundles or upsell items for recurring sales scenarios.

## Description
One row represents a single optional product line item associated with a specific sales order template. It defines the quantity and unit of measure for an optional item that a customer may choose to include when a template is applied to a new quotation. This is a raw landing table in the staging layer, representing a direct snapshot of the source system's configuration data.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| sale_order_template_id | INTEGER | false | Foreign key to the parent template | Links to the main template definition. |
| company_id | INTEGER | true | Multi-company identifier | Identifies the business unit owning this option. |
| product_id | INTEGER | false | Product identifier | Reference to the product catalog. |
| uom_id | INTEGER | false | Unit of Measure identifier | The unit in which the quantity is expressed. |
| create_uid | INTEGER | true | Creator user ID | ID of the user who created the record. |
| write_uid | INTEGER | true | Last modifier user ID | ID of the user who last updated the record. |
| name | JSONB | false | Display name/description | Likely contains localized strings for the option. |
| quantity | NUMERIC | false | Quantity of the product | The amount of the product to be added. |
| create_date | TIMESTAMP | true | Creation timestamp | Recorded by the ingestion job. |
| write_date | TIMESTAMP | true | Last update timestamp | Recorded by the ingestion job. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `sale_order_template_id` → `sale_order_template.id` (Inferred from naming convention).
    - `product_id` → `product_product.id` (Standard Odoo relationship).
    - `uom_id` → `uom_uom.id` (Standard Odoo unit of measure relationship).
    - `company_id` → `res_company.id` (Standard Odoo multi-company relationship).
- **Natural keys (inferred):** Not confidently inferable.

## Caveats for downstream consumers

- **Sensitive Data:** Contains user IDs (`create_uid`, `write_uid`) which may need to be joined against a user directory for PII resolution.
- **Timestamps:** Assumed to be in UTC, consistent with standard Odoo database configurations.
- **Data Integrity:** The `name` column is `JSONB`; ensure your downstream processing handles nested keys (e.g., `name->>'en_US'`) to extract human-readable text.
- **Soft Deletes:** This table does not appear to have an explicit `active` flag, but Odoo tables often rely on external logic for lifecycle management; verify if records are purged or simply orphaned.