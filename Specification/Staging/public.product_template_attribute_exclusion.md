# product_template_attribute_exclusion

## Source system
This table originates from an Odoo ERP system. The naming convention (e.g., `product_template_attribute_exclusion`, `product_tmpl_id`, `create_uid`, `write_uid`) is characteristic of Odoo's ORM-generated database schema, which uses these specific audit and relationship columns across its product configuration modules.

## Functional process 
This table supports the product configuration and variant management process. It defines constraints or exclusions between product attributes, ensuring that certain combinations of attribute values cannot be selected together for a specific product template, which is critical for complex product catalogs where some configurations are technically or commercially invalid.

## Description
One row in this table represents a single exclusion rule applied to a specific product template, preventing the selection of a particular attribute value in combination with others. As a staging table, it serves as a raw, landed copy of the Odoo database state, capturing the configuration logic at the grain of one exclusion rule per product template.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Sequence-generated by Odoo. |
| product_template_attribute_value_id | INTEGER | true | Foreign key to the excluded attribute value | Represents the specific value that is restricted. |
| product_tmpl_id | INTEGER | false | Foreign key to the product template | The product template to which this exclusion rule applies. |
| create_uid | INTEGER | true | User ID who created the record | References the `res.users` table. |
| write_uid | INTEGER | true | User ID who last modified the record | References the `res.users` table. |
| create_date | TIMESTAMP | true | Record creation timestamp | UTC timestamp. |
| write_date | TIMESTAMP | true | Record last modification timestamp | UTC timestamp. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):**
    - `product_tmpl_id` → `product_template.id`: This column links the exclusion rule to the specific product template definition.
    - `product_template_attribute_value_id` → `product_template_attribute_value.id`: This column identifies the specific attribute value being excluded.
    - `create_uid` / `write_uid` → `res_users.id`: These columns track the administrative users responsible for the configuration.
- **Natural keys (inferred):** Not confidently inferable from the provided metadata.

## Caveats for downstream consumers

- **Timestamps:** Timestamps are assumed to be in UTC, consistent with standard Odoo deployment practices.
- **Soft Deletes:** This table does not appear to have a dedicated `active` or `deleted_at` flag; however, Odoo often uses an `active` boolean column in other tables. Check for the absence of such a column if filtering for "active" rules.
- **Nullability:** `product_template_attribute_value_id` is nullable, which may imply that some exclusion rules are defined at a broader level or are currently incomplete/misconfigured in the source.
- **Audit Columns:** `create_uid` and `write_uid` are internal Odoo identifiers and do not contain human-readable names; they must be joined against the `res_users` table to identify the actual users.