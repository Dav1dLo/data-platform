# crm_lead_pls_update_crm_lead_scoring_frequency_field_rel

## Source system
The table likely originates from a custom CRM or a marketing automation platform, given the specific naming convention referencing "lead scoring frequency" and "pls update" (potentially referring to Predictive Lead Scoring). The naming pattern suggests a relational mapping table generated by an internal application or a middleware integration layer.

## Functional process 
This table supports the lead management and scoring process by maintaining a many-to-many relationship between lead update events and lead scoring frequency configurations. It enables the system to track which scoring frequency parameters were applied to or associated with specific lead update records.

## Description
One row in this table represents a single association between a lead update event and a scoring frequency configuration. As a staging table, it serves as a raw, normalized link entity used to resolve many-to-many relationships between the primary lead update records and their associated scoring frequency metadata.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| crm_lead_pls_update_id | INTEGER | false | Foreign key to the lead update event record | Represents the source entity in the relationship. |
| crm_lead_scoring_frequency_field_id | INTEGER | false | Foreign key to the scoring frequency configuration | Represents the target entity in the relationship. |

## Keys

- **Primary key (inferred):** Not confidently inferable from the provided metadata; likely a composite key of both columns.
- **Foreign keys (inferred):** 
    - `crm_lead_pls_update_id` → `crm_lead_pls_update.id` (guess: links to the primary lead update table).
    - `crm_lead_scoring_frequency_field_id` → `crm_lead_scoring_frequency_field.id` (guess: links to the scoring frequency definition table).
- **Natural keys (inferred):** Not confidently inferable.

## Caveats for downstream consumers

- This table is a pure join/link table; it contains no descriptive attributes, only identifiers.
- There is no audit timestamp or soft-delete flag present; assume this table represents the current state of associations as captured during the last ingestion.
- Ensure referential integrity is checked against the parent tables, as staging tables may contain orphaned records if the ingestion process is not atomic.