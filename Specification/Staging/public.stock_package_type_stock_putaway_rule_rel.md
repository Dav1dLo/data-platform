# stock_package_type_stock_putaway_rule_rel

## Source system
This table likely originates from an Odoo ERP or a similar modular inventory management system. The naming convention `_rel` combined with the two foreign key columns is characteristic of a many-to-many join table generated by an ORM (Object-Relational Mapper) to link stock putaway rules with specific package types.

## Functional process 
This table supports the inventory management and warehouse automation process. It defines the relationship between specific packaging configurations (e.g., pallets, boxes, crates) and the putaway rules that dictate where those packages should be stored within a warehouse facility.

## Description
One row in this table represents a single association between a specific stock putaway rule and a stock package type. It serves as a raw landing copy of a junction table, enabling the mapping of multiple package types to multiple storage rules to facilitate automated warehouse routing.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| stock_putaway_rule_id | INTEGER | false | Foreign key to the putaway rule definition | Links to the primary rule configuration. |
| stock_package_type_id | INTEGER | false | Foreign key to the package type definition | Identifies the specific container or packaging unit. |

## Keys

- **Primary key (inferred):** Not confidently inferable; likely a composite primary key consisting of both `(stock_putaway_rule_id, stock_package_type_id)`.
- **Foreign keys (inferred):** 
    - `stock_putaway_rule_id` → `stock_putaway_rule.id`: This column references the master table defining storage logic.
    - `stock_package_type_id` → `stock_package_type.id`: This column references the master table defining packaging dimensions and types.
- **Natural keys (inferred):** The combination of `(stock_putaway_rule_id, stock_package_type_id)` acts as the business key for this relationship.

## Caveats for downstream consumers

- This is a junction table; queries should expect a many-to-many relationship between the two parent entities.
- There are no timestamps or audit columns present in this table; it is purely structural.
- Ensure that joins to parent tables are handled as `INNER JOIN` if you only require valid, active relationships, or `LEFT JOIN` if you are auditing orphaned associations.