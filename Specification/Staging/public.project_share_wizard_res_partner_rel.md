# project_share_wizard_res_partner_rel

## Source system
This table originates from an Odoo ERP system. The naming convention `res_partner` is the standard internal identifier for the "Partner" (customer/vendor/contact) entity in Odoo, and the `_rel` suffix indicates a join table generated by the Odoo ORM to manage a many-to-many relationship.

## Functional process 
This table supports the "Share" or "Collaboration" process within project management modules. It links specific project sharing wizard instances to the partners (users or contacts) who have been granted access or invited to collaborate on those projects.

## Description
One row represents a single association between a project sharing wizard instance and a partner. It is a raw landing copy of a many-to-many join table, used to resolve the relationship between project sharing configurations and the entities involved in that share.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| project_share_wizard_id | INTEGER | false | Foreign key to the project share wizard instance | Links to the configuration of the share event. |
| res_partner_id | INTEGER | false | Foreign key to the partner entity | Identifies the contact or user involved in the share. |

## Keys

- **Primary key (inferred):** Not confidently inferable; likely a composite primary key on `(project_share_wizard_id, res_partner_id)`.
- **Foreign keys (inferred):** 
    - `project_share_wizard_id` → `project_share_wizard.id`: This column references the parent wizard configuration record.
    - `res_partner_id` → `res_partner.id`: This column references the partner record in the core contact management table.
- **Natural keys (inferred):** Not confidently inferable.

## Caveats for downstream consumers

- This is a junction table; it contains no descriptive attributes, only identifiers.
- There is no audit timestamp (e.g., `create_date`) present in this table, making it difficult to determine when the relationship was established.
- As a staging table, it should be joined with the corresponding master tables to retrieve meaningful business names or project details.