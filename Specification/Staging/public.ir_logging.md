# ir_logging

## Source system
This table originates from an Odoo ERP system. The naming convention (e.g., `create_uid`, `write_uid`, `create_date`, `write_date`) and the specific structure of the logging fields are characteristic of Odoo's internal audit and logging framework.

## Functional process 
This table supports system observability and auditability, capturing application-level logs generated during business process execution. It tracks internal events, errors, or system messages, providing a trace of activity across various modules within the ERP environment.

## Description
One row represents a single log entry generated by the application, capturing the context of the event including the function, file path, and severity level. As a staging table, it serves as a raw, landed copy of the application's internal logging database, intended for diagnostic analysis or audit reporting.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| id | INTEGER | false | Surrogate primary key | Uses sequence `ir_logging_id_seq`. |
| create_uid | INTEGER | true | User ID who created the log entry | References the user table. |
| write_uid | INTEGER | true | User ID who last updated the log entry | References the user table. |
| name | VARCHAR | false | Name or identifier of the log event | Often contains a short description or category. |
| type | VARCHAR | false | Type of log entry | e.g., 'server', 'client', 'error'. |
| dbname | VARCHAR | true | Database name where the log originated | Useful in multi-tenant Odoo environments. |
| level | VARCHAR | true | Severity level of the log | e.g., 'INFO', 'WARNING', 'ERROR'. |
| path | VARCHAR | false | File path of the source code | Indicates the module or file triggering the log. |
| func | VARCHAR | false | Function name | The specific code function that generated the log. |
| line | VARCHAR | false | Line number in the source code | String representation of the code line. |
| message | TEXT | false | Detailed log message content | The primary payload of the log event. |
| create_date | TIMESTAMP | true | Timestamp of log creation | Assumed UTC. |
| write_date | TIMESTAMP | true | Timestamp of last update | Assumed UTC. |

## Keys

- **Primary key (inferred):** `id`
- **Foreign keys (inferred):** 
    - `create_uid` → `res_users.id` (Guess: standard Odoo pattern for record creation tracking).
    - `write_uid` → `res_users.id` (Guess: standard Odoo pattern for record modification tracking).
- **Natural keys (inferred):** Not confidently inferable.

## Caveats for downstream consumers

- **Timestamps:** Timestamps are assumed to be in UTC, consistent with standard Odoo deployment practices.
- **Data Retention:** This table is likely subject to high-volume inserts and potential periodic truncation or archiving; query performance may degrade over time without proper indexing on `create_date`.
- **Sensitive Data:** The `message` column may contain PII or sensitive system configuration details depending on the log level; ensure appropriate masking if exposing to non-admin users.
- **Soft Deletes:** This table does not appear to implement a soft-delete flag; records are typically immutable once written.