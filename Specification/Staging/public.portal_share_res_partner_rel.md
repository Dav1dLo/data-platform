# portal_share_res_partner_rel

## Source system
This table originates from an Odoo ERP system. The naming convention `res_partner` is the standard internal identifier for the "Partner" (customer/vendor/contact) model in Odoo, and the `_rel` suffix indicates a standard many-to-many join table generated by the Odoo ORM to link portal sharing configurations to specific partners.

## Functional process 
This table supports the customer portal access management process. It maps specific portal share instances (which define access rights or shared documents) to the individual partners (users or contacts) who have been granted access to those resources.

## Description
One row in this table represents a single association between a portal share record and a partner record. It serves as a raw landing of the join table used to resolve many-to-many relationships between portal access configurations and business partners within the staging layer.

## Columns

| Column | Type | Nullable | Meaning | Notes |
| :--- | :--- | :--- | :--- | :--- |
| portal_share_id | INTEGER | false | Foreign key to the portal share definition | Links to the parent portal configuration. |
| res_partner_id | INTEGER | false | Foreign key to the partner record | Identifies the contact or user granted access. |

## Keys

- **Primary key (inferred):** Not confidently inferable; likely a composite primary key on `(portal_share_id, res_partner_id)`.
- **Foreign keys (inferred):** 
    - `portal_share_id` → `portal_share.id` (Inferred from Odoo naming conventions).
    - `res_partner_id` → `res_partner.id` (Inferred from Odoo naming conventions).
- **Natural keys (inferred):** The combination of `(portal_share_id, res_partner_id)` acts as the unique business key for this relationship.

## Caveats for downstream consumers

- This table is a pure join table; it contains no descriptive attributes, only identifiers.
- There is no audit timestamp or soft-delete flag present; assume this table reflects the current state of relationships as captured during the last ingestion.
- Ensure that joins to `res_partner` and `portal_share` are handled as inner joins if you only require active, valid relationships.